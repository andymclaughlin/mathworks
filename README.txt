To compile the Fraction test program use cd to navigate to the folder containing this file and execute the make command.

To run the program that tests the Fraction class execute the ./run.sh command.
